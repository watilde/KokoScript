# KokoScript 設計思想

## コンセプト
「もし日本人がJavaScriptを作ったら」という世界観を表現する。
句読点を活用した自然な日本語の文章のようなコード。

## 構文設計

### 句読点の役割
- **読点（、）**: キーワードの後、節の区切り
- **句点（。）**: 文の終わり
- **鉤括弧（「」）**: 文字列リテラル

### 基本原則
1. キーワードの後には必ず読点（、）を置く
2. 文の終わりには必ず句点（。）を置く
3. 識別子とキーワードは句読点で区切られる
4. スペースは不要（あっても良い）

## 構文例

### 変数
```kokoscript
変数、名前は「田中」。
変数、年齢は25。
```

### 条件分岐
```kokoscript
もし、年齢が20より大きいならば、
  「成人です」を表示。
そうでなければ、
  「未成年です」を表示。
終わり。
```

### 繰り返し
```kokoscript
3回、繰り返す、
  「こんにちは」を表示。
終わり。
```

### 関数
```kokoscript
関数、挨拶は、名前で、
  「こんにちは」と名前を表示。
終わり。

挨拶に「太郎」を渡す。
```

### 演算
```kokoscript
変数、合計は0。
合計は合計に10を足す。
合計は合計から5を引く。
合計は合計に2を掛ける。
合計は合計を2で割る。
```

## JavaScriptとの対応

| KokoScript | JavaScript |
|------------|------------|
| 変数、xは10。 | let x = 10; |
| もし、x > 5ならば、 | if (x > 5) { |
| 3回、繰り返す、 | for (let i = 0; i < 3; i++) { |
| 関数、名前は、引数で、 | function 名前(引数) { |
| 「文字列」を表示。 | console.log("文字列"); |
| 値を返す。 | return 値; |

## 予約語リスト

### 制御構造
- 変数、もし、そうでなければ、終わり
- 繰り返す、回、抜ける、続ける
- 関数、返す

### 演算子（自然言語）
- は（代入）
- が（比較の主語）
- と（連結）
- を（目的語）
- に（方向）
- で（手段）
- より、以上、以下、等しい
- 大きい、小さい

### アクション
- 表示、渡す、呼ぶ
- 足す、引く、掛ける、割る

### リテラル
- 真、偽、空
